$(document).ready(function () {



    /*function getList(current_page) {
        var postData = {};
        postData.page = current_page;
        postData.per_page = $("#per_page").val();
        postData.search = $("#search").val();
        postData.order_by = $("#order_by").val();
        postData.price_min = $( "#slider-range" ).slider( "values", 0 );
        postData.price_max = $( "#slider-range" ).slider( "values", 1 );

        $.get( "public/product/list/", postData, function( data ) {
            $("#product_listing").html(data);
        },"html");
    }
    $("#get_list_button").on("click",function () {
        getList(0);
    });
    $("body").on("click",".page_product",function () {
        var current_page = $(this).children("span").attr("data-page");
        getList(current_page);
    });*/

});